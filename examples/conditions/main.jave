outy seq Foremost<> --> <<nada>> {
    allow vag Foo 2b=2 0.6;;

    maybe (<Foo bigly 0.5>) -> {
        Pront("Over half");;
    } furthermore (<Foo lessly 0.5>) -> {
        Pront("Under half");;
    } otherwise -> {
        Pront("Exactly half");;
    }

    give up;;
}
